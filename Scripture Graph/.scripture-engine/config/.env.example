# Copy to .env (gitignored). Loaded into the engine's environment.
# OPENAI_API_KEY=sk-...        # only if using OpenAI embeddings
# CLAUDE_CODE_OAUTH_TOKEN=...  # from `claude setup-token` (alternative to `claude /login`)
