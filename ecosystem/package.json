{
  "name": "scripture-graph-ecosystem",
  "private": true,
  "version": "0.1.0",
  "description": "Scripture Graph plugin suite + collaboration backend (Subsinium Vault ecosystem)",
  "workspaces": [
    "packages/core-sdk",
    "plugins/scripture-graph",
    "server"
  ],
  "scripts": {
    "build": "npm run build -w packages/core-sdk && npm run build -w plugins/scripture-graph && npm run build -w server",
    "test": "npm run test -w packages/core-sdk && npm run test -w server",
    "dev:server": "npm run dev -w server"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
